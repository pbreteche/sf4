{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ article.title }}</h1>
    <p>Écrit par {{ article.writtenBy.name }}</p>
    <div>{{ article.content }}</div>
{% endblock %}

{% block title %}
{{ article.title | title }}
{% endblock %}

